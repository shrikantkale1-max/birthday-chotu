<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Happy Birthday Swara 🎉</title>

<style>
body {
    margin: 0;
    height: 100vh;
    background: radial-gradient(circle, #ff9a9e, #fad0c4);
    overflow: hidden;
    font-family: 'Comic Sans MS', cursive;
    text-align: center;
}

/* TEXT */
h1 {
    margin-top: 40px;
    font-size: 3.5rem;
    font-weight: bold;
    color: #fff;
    text-shadow: 0 0 10px gold, 0 0 20px orange;
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0% {transform: scale(1);}
    50% {transform: scale(1.1);}
    100% {transform: scale(1);}
}

/* CAKE */
.cake {
    position: relative;
    width: 200px;
    height: 120px;
    background: #ff6f61;
    border-radius: 10px;
    margin: 80px auto;
}

.cake::before {
    content: "";
    position: absolute;
    width: 100%;
    height: 30px;
    background: #fff;
    top: -30px;
    border-radius: 10px 10px 0 0;
}

.candle {
    position: absolute;
    width: 12px;
    height: 40px;
    background: white;
    top: -70px;
    left: 50%;
    transform: translateX(-50%);
}

.flame {
    position: absolute;
    width: 14px;
    height: 20px;
    background: gold;
    border-radius: 50%;
    top: -20px;
    left: -1px;
    animation: flicker 0.3s infinite alternate;
}

@keyframes flicker {
    from {transform: scale(1);}
    to {transform: scale(1.2);}
}

/* FIREWORKS */
.firework {
    position: absolute;
    width: 8px;
    height: 8px;
    background: yellow;
    border-radius: 50%;
    animation: explode 1s linear forwards;
}

@keyframes explode {
    from {
        transform: scale(1);
        opacity: 1;
    }
    to {
        transform: scale(15);
        opacity: 0;
    }
}

/* BUTTON */
button {
    padding: 15px 25px;
    font-size: 18px;
    border: none;
    border-radius: 20px;
    background: gold;
    cursor: pointer;
    box-shadow: 0 0 15px orange;
}
</style>
</head>

<body>

<h1>🎉 HAPPY BIRTHDAY<br>Swara (Chotu Don) 🎂</h1>

<div class="cake">
    <div class="candle">
        <div class="flame"></div>
    </div>
</div>

<button onclick="startParty()">🎵 Start Birthday Party 🎆</button>

<!-- MUSIC -->
<audio id="music">
    <source src="https://www.soundjay.com/human/sounds/happy-birthday-song-01.mp3" type="audio/mpeg">
</audio>

<script>
function startParty() {
    document.getElementById("music").play();
    setInterval(createFirework, 300);
}

function createFirework() {
    const fw = document.createElement("div");
    fw.className = "firework";
    fw.style.left = Math.random() * window.innerWidth + "px";
    fw.style.top = Math.random() * window.innerHeight + "px";
    fw.style.background = `hsl(${Math.random()*360},100%,50%)`;
    document.body.appendChild(fw);

    setTimeout(() => {
        fw.remove();
    }, 1000);
}
</script>

</body>
</html>
